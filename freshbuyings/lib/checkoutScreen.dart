import 'package:flutter/material.dart';
import 'package:freshbuyings/components/checkOutBody.dart';

class CheckOutScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: CheckOutBody(),
    );
  }
}
